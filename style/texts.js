import { StyleSheet } from 'react-native'
import { white } from './colors'

export const texts = StyleSheet.create({
  centerWhite: {
    color: white, 
    textAlign: 'center',
  },
  centerBold: {
    fontSize: 20, 
    textAlign: 'center', 
    fontWeight: 'bold',
  },
})